# Append-only tombstone ledger: git's union merge driver is exactly the
# right semantics for two branches appending deletions concurrently.
DELETIONS.md merge=union
# flutter_agent_memory merge drivers
