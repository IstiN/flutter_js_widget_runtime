---
id: "n_0037_44af"
type: "note"
title: "fa_widgets PR #6 checks fail because the publish tool REGENERATES .gitmodules from the widget catalog instead of merging: it dropped the e2e-scratch entry while its gitlink remained, causing 'fatal: No url found for submodule path' on checkout; fix is appending the entry back, and the tool must merge entries (or git rm --cached removed gitlinks) or every future publish PR will break the same way."
author: "agent"
date: "2026-09-12T19:34:47.261131Z"
area: "project"
topics: []
source: "agent"
accessCount: 0
importance: 0.8
tags: ["#note", "#source_agent", "submodule", "ci"]
---


# Note: n_0037_44af

fa_widgets PR #6 checks fail because the publish tool REGENERATES .gitmodules from the widget catalog instead of merging: it dropped the e2e-scratch entry while its gitlink remained, causing 'fatal: No url found for submodule path' on checkout; fix is appending the entry back, and the tool must merge entries (or git rm --cached removed gitlinks) or every future publish PR will break the same way.

**By:** [[agent]]
**Date:** 2026-09-12T19:34:47.261131Z
**Area:** [[project|project]]
